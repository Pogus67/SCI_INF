#pragma once
#include <iostream>
#include <string>
#include <utility>

using namespace std;

class szyfro
{
public:
	string szyfrcezara(string a);
	string deszyfrcezara(string a);
	string szyfrprzest(string a);
	string szyfroba(string a);
	string szyfrRSA(string a);
};

